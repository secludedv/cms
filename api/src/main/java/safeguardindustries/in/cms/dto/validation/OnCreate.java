package safeguardindustries.in.cms.dto.validation;

/**
 * Validation group marker interface for create operations.
 * Fields annotated with this group are only validated during entity creation,
 * not during updates.
 */
public interface OnCreate {
}
